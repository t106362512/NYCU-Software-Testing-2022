{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Make all",
            "type": "shell",
            "command": "make clean && make all",
            "options": {
                "cwd": "${workspaceFolder}/llvm-lab/distribute/share",
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}